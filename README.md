# УИБО-БОТ

![CI](https://github.com/mekit54567/uiboshki-bot/actions/workflows/ci.yml/badge.svg)

Telegram-бот для группы УИБО-03-24 (РТУ МИРЭА): расписание, дедлайны (общие
от старосты + личные), решалка (Groq/DeepSeek/Gemini, включая решалку по
тексту лекций), синк с СДО, Telegram WebApp (Mini App), анонимная лента
группы.

История версий и что в них было — [CHANGELOG.md](CHANGELOG.md).
План развития по фазам — [PLAN.md](PLAN.md).

## Запуск

```bash
pip install -r requirements.txt
# переменные окружения — см. config.py (BOT_TOKEN обязателен, остальное опционально)
python bot.py

# отдельным процессом, если нужен WebApp:
uvicorn webapp.server:app --host 0.0.0.0 --port 8000
```

## Тесты

```bash
pip install -r requirements-dev.txt
pytest
```

Тесты реальные, не заглушки: настоящая SQLite (temp-файл на каждый тест),
настоящий роутинг aiogram через `Dispatcher.feed_update` (а не прямые
вызовы хендлеров — так один раз был пойман баг с порядком регистрации
хендлеров, который прямые вызовы не видели), настоящая HMAC-проверка
подписи Telegram WebApp. Сеть наружу (Telegram/Groq/Gemini/СДО) в тестах
не используется — все внешние вызовы либо чистые функции без сети, либо
замоканы точечно (`monkeypatch`) в конкретном тесте.

CI (`.github/workflows/ci.yml`) гоняет этот же набор на каждый push и
pull request в `main`.

Что тестами **не** покрыто (честно): фактический сетевой поход к
Groq/Gemini/СДО (нужны живые ключи/куки — тестировались вручную при
разработке, см. PLAN.md), парсинг реального HTML расписания МИРЭА,
APScheduler-таймеры (тестируется вызываемая ими функция, не сам cron).

## Мониторинг

`GET /health` на WebApp-сервере — для внешнего аптайм-чекера. Падение
хендлера в самом боте шлёт короткий алерт старосте в личку (с антиспам-
троттлингом 5 минут между алертами) — см. `bot.py`, `notify_starosta_on_error`.
