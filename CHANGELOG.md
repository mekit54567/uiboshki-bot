# Changelog УИБО-БОТ

Версии — по коммитам в `main`, каждая с тегом (`git tag`) и кодовым именем
(как это делают Ubuntu/Android/k8s — не переименовывают сами коммиты, а
вешают именованный тег на нужный коммит). Старые сообщения коммитов не
трогались и не будут: это уже отправленная в GitHub история, переписывать
её означало бы менять хэши и требовать force-push — риск не стоит того,
когда тег даёт тот же результат без единой потери.

Кодовые имена v0.1.0–v1.7.0 (кроме v0.3.0 «Академик» — так назвал сам
автор в коммите) — отдельные слова из двух песен, которые попросил
использовать владелец. Не цитаты и не хуки/припевы (это под запретом по
авторскому праву), просто отдельные существительные, которые в текстах
встречаются: буревестник, овцебык, гамбит, нарвал, барсук, фронт, девятый
вал, вулкан, гнездо, лазарет.

---

## Эпоха 0 — первый прототип (24–25 апреля 2026)

### v0.1.0 «Буревестник» — `53e40c0`
Первый коммит. Бот в базовом виде: решалка, расписание.

### v0.2.0 «Овцебык» — `c14202e`
Ночь метаний между AI-бэкендами (Gemini → фикс UTF-8 → Flash Lite → назад
на Groq за три часа) + диалоговый режим решалки без LaTeX-разметки.

### v0.3.0 «Академик» — `952efad`
Название придумано не мной — так назвал релиз сам автор в коммите.
Синк файлов, импорт дедлайнов, новое меню, действия, парсер расписания.
Дальше — неделя мелких патчей (turso и откат от неё, синк файлов, конфликт
хендлеров документов) до паузы в разработке на 4+ месяца.

---

## Эпоха 1 — фазовый релонч (21–22 сентября 2026, ~23 часа)

Начало — `011d918` "Add development plan": впервые появляется план по
фазам вместо точечных фиксов. С этого момента и до текущего коммита —
непрерывный 23-часовой спринт с независимым код-ревью в середине.

### v1.0.0 «Гамбит» — `c441a04`
Фаза 0–2: реальные баги, общие клавиатуры, роутинг по намерению,
анонимная лента группы. Точка, с которой разработка снова "в зачёте".

### v1.1.0 «Нарвал» — `75abd91`
Фаза 3–6: автосинк дедлайнов из СДО (через переиспользование сессионной
куки), диффы расписания и заметки к парам, DeepSeek как альтернативный
бэкенд решалки, полнотекстовый поиск по файлам (FTS5), `/clearsem`.

### v1.2.0 «Барсук на троне» — `e59be36`
Фаза 4 (продолжение): поиск преподавателя/аудитории по всему расписанию
университета, а не только своей группы.

### v1.3.0 «Фронт» — `0fc6617`
Фаза 7: уведомления в общий чат группы — утренний дайджест, диффы
расписания, модерируемая старостой публикация дедлайнов.

### v1.4.0 «Девятый вал» — `eb01414`
Фаза 8: Telegram WebApp (Mini App) — отдельный FastAPI-бэкенд с
HMAC-проверкой initData, фронтенд поверх тех же данных, что и в боте.

### v1.5.0 «Вулкан» — `e8be2dc`
Фаза 9: решалка по лекциям на Gemini (grounded — ответ привязан к
реальному тексту загруженных лекций), включая независимое код-ревью
(агент на Opus, без контекста моей работы, нашёл и исправил 9 реальных
багов — порядок роутеров aiogram, утечка API-ключа в текст ошибки,
незакрытые дыры в `/clearsem` и др.), диагностику geo-блока Gemini для РФ,
подбор модели/бюджета контекста под реальные квоты аккаунта.

### v1.6.0 «Гнездо» — `d8c1907`
Фаза 10: статус "выполнено" стал персональным, а не общим на всех;
дедлайны разделены на общие (от старосты/СДО) и личные (видны только
автору); права на удаление; фото → текст через ИИ при добавлении
дедлайна; строгая валидация формата времени; фикс кодировки Windows-1251
для .txt-лекций.

### v1.7.0 «Лазарет» — `b2a1090`
Фаза 11: тесты, CI, git-теги/changelog, базовый мониторинг/алертинг.
Показательный момент: сам CI на первом же прогоне поймал реальный баг —
троттлинг алертов старосте ложно срабатывал в первые ~5 минут жизни
процесса (`time.monotonic()` стартует от 0 на свежем контейнере, не от
эпохи). Без CI это ушло бы в прод. Подробности — в PLAN.md, Фаза 11.

---

## Как будет дальше

Новую версию = новый тег `vX.Y.Z` на нужном коммите + запись сюда.
Мажор — ломающие изменения схемы БД/API, минор — новая фича, патч — фикс.
