# План развития УИБО-БОТ

Черновик для себя (Claude) на следующую сессию, когда появится пуш через комп владельца.
Владелец дал добро добавлять свои идеи без согласования — если что-то не зайдёт, откатываем, не страшно.
Порядок ниже — предлагаемый приоритет, не жёсткий контракт: смещать по ходу дела нормально.

## Фаза 0 — фиксы, которые не требуют архитектурных решений

Можно делать в первую же сессию с пушем, риска нет, эффект сразу.

- [ ] `handlers/files.py`: `handle_sync_json` (`@router.message(F.document)`) перехватывает документы
      в любом FSM-состоянии, включая `HWAdd.content` из `announce.py`. Добавить
      `StateFilter(None)` или явную проверку `state.get_state() is not None: return` в начале.
      Баг: `/addhw` с прикреплённым файлом молча не срабатывает.
- [ ] Единый набор кнопок главного меню (сейчас размазан по `start.py`, `deadlines.py`, `files.py`,
      `solver.py` как локальные `MAIN_KB`/`BUTTON_TEXTS`, местами неполный — в `solver.py`
      `BUTTON_TEXTS` не хватает "📝 ДЗ", "🏆 Рейтинг", "⋯ Действия"). Вынести `MAIN_KB` и
      `MENU_BUTTON_TEXTS` в отдельный `keyboards.py`, и добавить общий guard на все FSM-состояния:
      если входящий текст — это текст кнопки меню, а не ожидаемый ввод, сбрасывать state и
      обрабатывать как обычную команду, а не глотать как значение поля.
- [ ] `STAROSTA_ID` — при `STAROSTA_ID == 0` сейчас все admin-команды (`/announce`, `/setzam`,
      `/importdeadlines`, `/syncfiles`) открыты всем. Либо падать при старте, если переменная не
      задана, либо явно логать warning уровня critical при `init`.
- [ ] `database.py`: при старте выставить `PRAGMA journal_mode=WAL;` — снижает риск
      `database is locked` при параллельных cron-джобах (scheduler.py гоняет 3 job'а) + хендлерах.
- [ ] `schedule_parser.fetch_schedule_raw()` дергается без кэша отовсюду (каждую минуту из
      `check_lesson_reminders`, плюс из каждой команды). Добавить простой TTL-кэш (5–10 минут) —
      разгружает и бота, и стороннего провайдера ical.
- [ ] Мёртвый `GROQ_MODEL` в `config.py` — не используется, `groq_solver.py` хардкодит свои
      модели напрямую. Убрать или реально прокинуть.

## Фаза 1 — смена интерфейса: от дерева клавиатур к свободному вводу

Идея: держать текущие команды/кнопки как fallback, но дать основной путь через
естественный язык поверх уже существующего Groq-клиента.

- [ ] Маленький intent-роутер: короткий системный промпт классифицирует свободный текст
      в один из существующих обработчиков (расписание/дедлайны/файлы/решить/ДЗ/голосование)
      + извлекает параметры (дата, предмет). Один дешёвый вызов перед основной логикой.
  Fallback — если классификатор не уверен, гонит в решалку как раньше (`handle_plain_text`).
- [ ] Постепенно убирать жёсткие FSM-цепочки там, где это не критично (добавление дедлайна,
      выбор предмета для решателя) — заменять на разбор одной фразой ("добавь дедлайн по
      матстату на 30.05 сдача лабы").
- [ ] Не трогать FSM там, где нужен строгий пошаговый ввод (загрузка файла с бинарным вложением).

## Фаза 2 — killer feature: анонимная лента группы

Главная гипотеза сессии: то, чего не хватает — не улучшение утилити-функций, а социальный
слой, который дают открывать бота без повода (см. кейс "Кампус" — анонимная лента = 40% DAU).

- [ ] Новая таблица `feed_posts` (id, text, photo_file_id, author_id — не показывается,
      created_at, reply_count).
- [ ] `/anon` (или новая команда `/feed`/`⋯ Подслушано`) — пишет не старосте лично,
      а постит анонимно в общий чат группы (нужен `chat_id` группы в конфиге,
      не только `STAROSTA_ID`).
- [ ] Реакции/комменты — через reply в Telegram на пост бота, либо через inline-кнопки
      👍/😂/🔥 с счётчиком в БД.
- [ ] Продумать анти-спам/модерацию — простая: rate-limit 1 пост в N минут на юзера,
      плюс возможность старосты удалить пост (`/delpost ID` или кнопка).
- [ ] Оставить старый приватный `/anon → старосте` как отдельную опцию — не всё должно быть
      публичным (жалобы конкретно старосте лично).

## Фаза 3 — расписание как источник правды, а не просто вывод (готово)

- [x] Диффы расписания: `schedule_snapshots` (дата → JSON событий), `schedule_diff.py`
      сравнивает при каждом interval-джобе (раз в `SCHEDULE_DIFF_CHECK_MINUTES`, по умолчанию
      20 мин), шлёт пуш подписчикам только при реальном изменении (аудитория/время/отмена/
      новая пара). Проверено и синтетическим тестом (3 типа изменений), и живым прогоном
      на реальном ical-фиде (0 false positive на втором подряд запуске).
- [x] Заметки на пару: таблица `lesson_notes`, команда `/note` — либо одной строкой
      (`/note сегодня Матан: контрольная в 401`), либо пошагово через инлайн-кнопки
      (день → текст). Выводятся под расписанием в `/schedule` и `/tomorrow`.
      Проверено функциональным тестом (парсинг короткой формы + сохранение + вывод блока).

## Фаза 4 — СДО и внешние данные

- [x] Разведка логина: `login.mirea.ru` — простой Django POST без капчи. Реальная СДО
      (`online-edu.mirea.ru`, Moodle) авторизуется через `sso.mirea.ru` (Keycloak) с 2FA
      (код в Max по умолчанию, либо на почту, либо QR-логин) — полную автоматизацию входа
      сочли нецелесообразной (2FA не обойти без живого человека или доступа к его Max/почте).
- [x] Выбранный подход: переиспользование куки `MoodleSession`, полученной один раз обычным
      логином в браузере (с "запомнить меня"). Протестировано живьём: кука даёт полный доступ
      к `/calendar/view.php?view=upcoming`, `/grade/report/overview/index.php` и т.д.
- [x] `sdo_parser.py` — тянет `/calendar/view.php?view=upcoming`, фильтрует
      `data-event-component="mod_assign"` + `data-event-eventtype="due"`, парсит через
      BeautifulSoup (data-атрибуты + unix-таймстамп из ссылки "Когда" — без регулярок по
      русским датам). Протестировано на реальных данных — 2 реальных дедлайна распарсились
      корректно, повторный запуск верно задедуплицировал по `external_id`.
- [x] `database.py`: колонка `external_id` в `deadlines` (+миграция для старых баз),
      `get_deadline_by_external_id` для дедупа.
- [x] `scheduler.py`: джоба `sync_sdo_deadlines` каждые `SDO_SYNC_INTERVAL_HOURS` (по
      умолчанию 6ч), уведомляет старосту при протухшей куке (без спама — флаг, чтобы не
      слать одно и то же каждый цикл) и при добавлении новых дедлайнов.
- [ ] `/syncsdo` (ручной триггер, есть) — протестирован только через прямой вызов
      `sync_deadlines()`, не через реальный Telegram update (нет живого бота/токена для
      end-to-end теста прямо сейчас).
- [ ] Не решено: как часто на практике протухает MoodleSession — узнаем эмпирически после
      деплоя. Если окажется, что живёт день-два — раз в несколько дней надо будет заходить
      руками в браузер и обновлять SDO_SESSION_COOKIE. Если реально долго (недели, судя по
      "запомнить меня") — почти не потребует внимания.
- [x] Поиск преподавателей/аудиторий по всему университету — итог. Первые две попытки
      (Blazor WASM на /schedule/api/ical, чужой id только вручную) уткнулись в тупик, третья
      сработала: владелец руками поймал через DevTools на `lk.mirea.ru` реальный сетевой
      запрос поиска — оказалось, что сам виджет там бьёт не в lk.mirea.ru (это просто Bitrix
      для личного кабинета), а в отдельный ПУБЛИЧНЫЙ бэкенд без авторизации:
      `https://schedule-of.mirea.ru/schedule/api/search?limit=N&match=текст` — ищет сразу по
      группам/преподам/аудиториям, различаются полем `scheduleTarget` (1/2/3). Плюс
      `/baseinfo?id=&type=` и `/ical/{type}/{id}` — тот же движок, что и ICAL_URL, просто
      без прокладки через english.mirea.ru.
      `mirea_schedule_api.py` — обёртка (search_targets/get_baseinfo/fetch_ical).
      `/teacher ФИО`, `/room номер` — ищут по ВСЕЙ базе МИРЭА (не только своя группа),
      при нескольких совпадениях — инлайн-кнопки на выбор, дальше тянется ical именно
      этого препода/аудитории (их полное расписание, не только пересечение с моей группой)
      на 2 недели вперёд.
      Проверено на реальных живых данных (не моках): поиск (уникальное имя/фамилия
      с десятком тёзок/номер аудитории/пустой и несуществующий запрос), baseinfo, fetch
      несуществующего id (аккуратный None вместо краша), и на уровне хендлеров —
      однозначный результат/неоднозначный с кнопками/клик по кнопке/нет совпадений.

## Фаза 5 — решалка (готово)

- [x] DeepSeek как альтернативный backend в `groq_solver.py` (`solve_text`/`solve_with_history`
      принимают `backend="groq"|"deepseek"`, свой URL/модель/ключ; если `DEEPSEEK_API_KEY` не
      задан — тихий фолбэк на Groq вместо ошибки). Фото остаётся только на Groq (llama-4-scout
      умеет в vision, DeepSeek chat — нет).
- [x] Выбор модели: команда/кнопка `/solve_ds` (тоже в ⋯ Действия → "🐋 Решить через DeepSeek"),
      либо префиксом в свободном тексте ("дипсик: реши задачу..."). Контекст (история диалога,
      привязка к предмету) сохраняется так же, как у обычной решалки.
      Проверено юнит-тестами на замоканном HTTP-клиенте: роутинг groq/deepseek по URL и ключу,
      фолбэк при отсутствующем ключе, парсинг префиксов "дипсик:"/"deepseek:"/"через дипсик:".

## Фаза 6 — по возможности, не приоритет

- [x] FTS5-поиск по файлам (title+subject+file_name) — команда `/search запрос`, external-content
      FTS5-таблица `files_fts` над `files` с триггерами (insert/update/delete держат индекс
      в синхроне без дублирования данных), плюс rebuild при апгрейде существующей базы.
      Проверено: поиск по названию/предмету/имени файла, пустой/несуществующий запрос,
      синхронизация индекса после удаления файла, устойчивость к спецсимволам FTS5-синтаксиса
      в запросе (кавычки/операторы не роняют поиск), бэкфилл индекса на "старой" базе без FTS.
- [ ] Слияние таблиц `deadlines` и `homework` в одну сущность — сейчас две параллельные
      системы одного и того же смысла ("что сдать к дате"), разделены по историческим причинам
      (староста/зам), это усложняет и код, и восприятие.
- [ ] Telegram WebApp для расписания/файлов вместо чат-сообщений с инлайн-кнопками — самая
      дорогая по трудозатратам идея, но даёт реальный визуальный отрыв от нативных приложений.

## Фаза 7 — уведомления в общий чат группы (готово)

- [x] Задача от владельца: бот должен не просто быть добавлен в группу, а слать туда
      ТОЛЬКО односторонние утилитарные уведомления (без чтения сообщений группы, без
      приватности отдельных пользователей — это нерешаемо в Telegram-группах в принципе).
      Всё остальное (решалка, файлы, дедлайны/ДЗ, /teacher, /room и т.д.) остаётся
      строго в личке с ботом, в группу не выводится.
- [x] Утренний дайджест в группу (`scheduler.send_group_morning_digest`) — расписание на
      сегодня + пометки к парам (`_notes_block`, переиспользован из handlers/schedule.py),
      без погоды (это личное, в группе лишнее). Молчит без исключений, если GROUP_CHAT_ID
      не настроен — не критическая функция. Повешен на тот же cron-слот, что и личная
      утренняя рассылка (SCHEDULE_HOUR:SCHEDULE_MINUTE).
- [x] Уведомления о смене/отмене пары в группу (`schedule_diff.check_schedule_changes`) —
      тот же текст диффа, что уходит подписавшимся в личку, теперь ещё и постится в
      GROUP_CHAT_ID (если задан), потому что личные подписки есть не у всех, а смену пары
      надо знать всем.
- [x] Дедлайны — ручная модерация старостой перед публикацией в группу, т.к. автосинк из
      СДО не всегда достоверен (см. Фазу 4). `scheduler.send_deadline_reminders`
      разбит на `_format_deadline_reminders` (общий текст) + сама рассылка подписчикам
      (как раньше, без изменений) + отдельно старосте с инлайн-кнопками
      "✅ Опубликовать в группу" / "🚫 Не публиковать" (`DEADLINE_POST_KB`).
      Обработчики `handlers/deadlines.py: deadline_post_confirm/deadline_post_decline`
      (callback_data `dlpost:yes`/`dlpost:no`) — публикуют исходный HTML-текст сообщения
      (`callback.message.html_text`, тем самым не теряя форматирование) в GROUP_CHAT_ID,
      правят сообщение старосте (дописывают статус, убирают кнопки), и защищены проверкой
      `from_user.id == STAROSTA_ID` (не староста — просто alert "Только для старосты",
      без побочных эффектов).
      Проверено вживую (temp SQLite + hand-rolled FakeBot/FakeCallback/FakeMessage,
      не моки библиотеки): подписчик получает обычное напоминание без кнопок, староста —
      то же самое + кнопки; не-староста жмёт кнопку → alert, ничего не публикуется и не
      меняется; староста жмёт "Опубликовать" → пост в группу с тем же HTML, сообщение
      старосте правится, кнопки исчезают; староста жмёт "Не публиковать" → в группу ничего
      не уходит, сообщение старосте правится на отказ.
- [ ] Режим приватности бота в группе (BotFather `/setprivacy`) — рекомендация: оставить
      ВКЛЮЧЁННЫМ (privacy mode ON), т.к. текущий скоуп — только исходящие от бота
      уведомления, бот не должен читать сообщения группы вообще. Решение/применение —
      на стороне владельца при регистрации бота в группе.

## Фаза 8 — Telegram WebApp / Mini App (готово, ждёт деплоя)

- [x] Задача от владельца: не дублировать чат-интерфейс бота, а дать реальную
      визуальную ценность — расписание/дедлайны/ДЗ/файлы одним экраном без
      команд, плюс отдельная фича-эксперимент (чат с DeepSeek с раскрываемым
      "ходом мыслей", как в приложениях DeepSeek/ChatGPT/Claude) на пробу на
      неделю. Дизайн-ориентир — Duolingo и общая практика Gen Z-интерфейсов
      (см. ниже), не супер-минимализм и не перегружено.
- [x] `webapp/server.py` — отдельный FastAPI-процесс (НЕ внутри bot.py,
      тот остаётся чистым long polling), делит с ботом ту же SQLite-базу и
      все существующие модули (`database.py`, `schedule_parser.py`,
      `groq_solver.py`) — ничего не задублировано. Эндпоинты: `/api/me`,
      `/api/schedule/{today,tomorrow,week,next}`, `/api/deadlines`
      (+`POST .../{id}/toggle` — чекбоксы; homework отдельной сущности в базе
      нет, "доска ДЗ" — это та же таблица `deadlines`, где `done` и есть
      чекбокс), `/api/files` (поиск/список — сами файлы отдаются не напрямую:
      Telegram `file_id` не резолвится в URL без `getFile` от лица бота,
      поэтому кнопка "Открыть" бьёт в `t.me/<bot>?start=file_<id>`),
      `/api/notes`, `/api/chat` (DeepSeek `deepseek-reasoner`, отдаёт отдельно
      `content` и `reasoning` — см. `groq_solver.chat_with_reasoning`).
- [x] `webapp/auth.py` — валидация Telegram `initData` (HMAC-SHA256 по
      `BOT_TOKEN`, ровно по алгоритму из офдоков Mini Apps) на каждый запрос
      через заголовок `X-Telegram-Init-Data`, с проверкой возраста (протухшая
      initData > 24ч — просим переоткрыть). Без валидной подписи — 401 везде.
- [x] `handlers/start.py`: диплинк `t.me/<bot>?start=file_<id>` (ловится
      `cmd_start_deeplink`, отдельно от обычного `/start`) — шлёт документ по
      file_id, это и есть механизм "открыть файл" из WebApp. `/app` — команда
      открытия WebApp из чата (кнопка не показывается, если `WEBAPP_URL` не
      настроен — не критично, ничего не падает). `keyboards.webapp_keyboard()`
      — общий хелпер кнопки.
      `database.py`: добавлен `set_deadline_done` (в отличие от
      `mark_deadline_done` — можно и обратно, 1→0, нужно для чекбоксов, где
      случайный клик надо уметь отменить) и `get_deadline`.
- [x] `webapp/static/index.html` — фронтенд: один файл (инлайн CSS/JS),
      Telegram WebApp SDK, тема подхватывается из `Telegram.WebApp.themeParams`
      (тёмная по умолчанию вне Telegram). 4 вкладки: Сегодня (bento-сетка —
      расписание + пометки к парам + счётчик дедлайнов + разворачиваемая
      неделя), Дедлайны (карточки с чекбоксами, цветные чипы дедлайна —
      просрочено/сегодня/завтра/позже, тап по карточке = toggle с haptic),
      Файлы (поиск с debounce, кнопка "Открыть" — диплинк в бота), Чат
      (история сообщений, `<details>` с ходом мыслей под каждым ответом,
      сворачивается/разворачивается).
      Референсы для дизайна и информационной плотности собраны заранее
      (Duolingo habit-loop, "Tactile Maximalism" Gen-Z UI тренд — Bento Grid,
      progressive disclosure вместо всего сразу, тактильные/объёмные кнопки,
      микро-анимации на смену состояния) — применены: сворачиваемые секции
      вместо простыни текста, чипы вместо голого текста статуса, haptic на
      тап, bento-карточки на главном экране вместо длинного списка.
- [x] `webapp/README.md` — что нужно для реального деплоя (HTTPS-хостинг
      отдельного uvicorn-процесса, `WEBAPP_URL` в окружении бота,
      регистрация `/newapp` в BotFather) — всё это по-прежнему на стороне
      владельца, как и договаривались.
      Протестировано вживую: py_compile + импорт всего проекта включая
      `webapp.*`; реальный HTTP через `uvicorn` на 127.0.0.1 (не только
      TestClient) — статика отдаётся (200), API без initData — 401, все
      маршруты видны в `/openapi.json`; `TestClient` с честно посчитанным
      HMAC initData (как считает сам Telegram) — `/api/me`, `/api/schedule/*`
      (в т.ч. реальный сетевой запрос к ical МИРЭА, не мок), `/api/deadlines`
      + toggle true→false→404 на несуществующем id, `/api/files`, `/api/notes`
      — всё через реальную temp-БД, не моки; диплинк `file_<id>` (существующий
      файл/несуществующий/пустой payload) и `/app` без `WEBAPP_URL` —
      через hand-rolled FakeBot/FakeMessage.
- [ ] Сам деплой (HTTPS-хостинг, `WEBAPP_URL`, `/newapp` в BotFather) —
      на стороне владельца, см. `webapp/README.md`.
- [ ] Тест DeepSeek-чата с реальным `DEEPSEEK_API_KEY` на живых запросах
      (сейчас проверен только путь до вызова API и обработка ошибки/502,
      сам вызов `deepseek-reasoner` не бил по реальному ключу — его нет
      в этом окружении).

## Фаза 9 — решалка по лекциям (Gemini, готово, ждёт GEMINI_API_KEY)

- [x] Задача от владельца: решать практику опираясь в первую очередь на текст
      реальных лекций предмета (не на общие знания модели), и делать это так,
      чтобы части задания, покрытые лекциями, решались "как в лекции", а
      непокрытые — обычным способом, с явной пометкой.
- [x] Разведка масштаба перед реализацией — не теория, а проверка на реальной
      лекции МИРЭА (23 стр. PDF, прислана владельцем): 55.7К символов текста,
      ~18-24К токенов на ОДНУ лекцию. При 8-16 лекциях на предмет — это
      150-380К токенов контекста. Проверил актуальные лимиты (веб-поиск,
      сентябрь 2026): Groq free tier (уже используется в боте) — 128K контекст
      и всего 6000 токенов/мин, физически не подходит. DeepSeek — новый V4 с
      1M контекстом пока не факт что уже на обычном API. Gemini free tier —
      контекст 1M токенов, 1500 запросов/день, 1M токенов/мин, бесплатно без
      карты — этого достаточно с большим запасом. Кэширование контекста на
      стороне Gemini (implicit и explicit) — платная фича, на free tier
      недоступна вообще — поэтому кэшируем не сторону Gemini, а сборку
      контекста у себя (см. ниже).
- [x] `file_text.py` — извлечение текста из PDF (pypdf)/DOCX (python-docx)/
      PPTX (python-pptx)/TXT. `extract_and_save(bot, db_file_id, tg_file_id,
      file_name)` качает файл по Telegram file_id и сохраняет текст в БД.
      Не делает OCR картинок (сканы без текстового слоя — пустая строка,
      файл просто не попадает в контекст лекций, сам файл никуда не девается).
      Ошибки не пробрасываются наружу — сбой парсинга не должен ломать сам
      аплоад/синк файла.
- [x] `database.py`: таблица `file_text` (file_id → content), `save_file_text`,
      `get_subject_lecture_context(subject)` — склеенный текст всех лекций
      предмета с заголовками-разделителями по файлу, `get_subjects_with_lecture_text()`
      — только предметы, где реально есть текст. `delete_file` теперь чистит
      и `file_text` (без этого текст удалённого файла молча остаётся в
      контексте предмета).
      Это и есть "кэш", который обсуждали: текст лекций парсится один раз при
      загрузке/синхронизации файла (`handlers/files.py`: и ручной /upload, и
      /syncfiles), а не при каждом решении задачи — при запросе к решалке
      просто читается уже готовая склейка из БД.
- [x] `gemini_solver.py` — отдельный бэкенд (не альтернатива Groq/DeepSeek,
      а другая задача): системный промпт прямо требует опираться на
      переданные лекции и явно помечать части ответа, не найденные в них.
      REST-вызов `generativelanguage.googleapis.com/v1beta/models/{model}:generateContent`
      (не через SDK — httpx, как и остальные бэкенды). `GEMINI_MODEL` —
      дефолт `gemini-2.5-flash`, задел на то, что имя модели у Google меняется
      быстрее, чем хотелось бы — если решалка ответит 404 на модель, поправить
      через переменную окружения, не через код.
- [x] `handlers/solver.py`: `/solve_lectures` — отдельная (не мульти-turn,
      в отличие от обычной решалки) машина состояний: выбор предмета ТОЛЬКО
      из тех, где есть загруженные лекции (иначе неоткуда взять контекст) →
      текст задания → ответ. Добавлено в ⋯ Действия и /help.
- [ ] Нужен `GEMINI_API_KEY` в переменных окружения (получить в Google AI
      Studio) — без него `/solve_lectures` вернёт понятную ошибку
      ("GEMINI_API_KEY не настроен на сервере"), это на стороне владельца.
- [ ] Не проверено на реальном ключе Gemini (нет ключа в этом окружении) —
      логика HTTP-запроса и разбора ответа протестирована на моках (happy
      path, пустые candidates/promptFeedback, пустой text/finishReason,
      форма запроса — url/params/systemInstruction/тело), но не на реальном
      сетевом вызове. Стоит один раз прогнать вживую после того как ключ
      появится.
      Протестировано вживую (temp SQLite, реальные PDF/DOCX/PPTX через
      reportlab/python-docx/python-pptx, не моки библиотек парсинга):
      extract_text на всех 4 форматах + неподдерживаемое расширение +
      битый PDF (не падает); extract_and_save через hand-rolled FakeBot;
      get_subject_lecture_context (склейка, порядок, заголовки, пустой
      предмет); get_subjects_with_lecture_text; delete_file чистит
      file_text; весь FSM `/solve_lectures` (нет лекций / несуществующий
      предмет / выбор предмета / отмена / реальный запрос с замоканным
      Gemini) через hand-rolled FakeMessage/FakeState.

---

Когда вернёшься к компу: сначала Фаза 0 (полчаса-час, чинит реальные баги), потом
обсудить вживую порядок Фаз 1–3 — social-фича (Фаза 2) технически простая, но нужно решение
по названию/тональности ("Подслушано" — это чужой бренд, придумать своё), Фаза 4 требует
сначала ручной разведки логина СДО прежде чем писать код.

---

## Фаза 10: Персональные/общие дедлайны, фото-OCR, строгий формат времени,
## фикс кодировки .txt

- [x] Статус "выполнено" у дедлайнов стал персональным, а не общим на всех
      (запрос владельца: "когда ты помечаешь дедлайн как выполненный, он
      должен отмечаться таковым только у тебя"). Новая таблица
      `deadline_done(deadline_id, user_id, done_at)`, старая колонка
      `deadlines.done` оставлена как легаси (никем больше не читается) —
      при первом создании таблицы в `init_db` существующие `done=1`
      переносятся в `deadline_done` на счёт старосты (`STAROSTA_ID`), чтобы
      история не терялась молча при обновлении.
      Работает и в боте (`/done`, персонально), и в WebApp (чекбокс,
      персонально через `x-telegram-init-data` → `user["id"]`).
- [x] Видимость дедлайнов: "общие" — созданные старостой (`created_by ==
      STAROSTA_ID`) или автосинком СДО (`created_by == 0`), их видят все.
      Всё, что добавляет обычный пользователь через `/add` (permission на
      добавление не менялась — она и раньше была открыта всем), видно
      только ему самому. `database.get_active_deadlines(viewer_id, ...)` /
      `get_deadlines_soon(...)` / `get_deadline_stats(viewer_id)` теперь
      обязательно принимают viewer_id и фильтруют по видимости.
      `database.is_shared_deadline(d)` — общий helper для проверки.
- [x] `/del`: теперь с правами — общий дедлайн может удалить только
      староста, личный чужой — только его автор (раньше `/del` мог удалить
      кто угодно что угодно). То же самое в `webapp/server.py`
      (`/api/deadlines/{id}/toggle` теперь 403 на попытку трогать чужой
      личный дедлайн — сам toggle по-прежнему персонален, так что для общих
      дедлайнов доступен всем).
- [x] `scheduler.send_deadline_reminders`: личная рассылка подписчикам
      теперь собирается персонально на каждого (общие + его личные, не
      отмеченные им самим), а не один и тот же текст всем. Пост в общий чат
      группы (через старосту, `dlpost:yes`/`dlpost:no`) собирается отдельным
      запросом `shared_only=True` — туда никогда не попадёт чей-то личный
      дедлайн.
- [x] Фото при добавлении дедлайна (шаг "описание"): если вместо текста
      прислали фото — текст с него распознаёт ИИ (`groq_solver.
      extract_text_from_image`, Groq llama-4-scout vision, OCR-промпт без
      решения задачи) и подставляет как описание. Зарегистрирован отдельным
      хендлером `@router.message(AddDeadline.description, F.photo)` ПЕРЕД
      текстовым — порядок важен (см. баг с catch-all в Фазе 9), здесь
      специфичный фильтр идёт первым, общий — вторым, как и положено.
- [x] Строгий формат времени в `/add`: было — регэксп проверял только форму
      ЧЧ:ММ, реально принимал "25:99"; стало — валидируется диапазон (часы
      00-23, минуты 00-59), плюс разделитель "." тоже принимается (с
      телефона часто набирают точкой) и нормализуется в ЧЧ:ММ.
- [x] `.txt`-лекции в кодировке Windows-1251 (обычный блокнот на русской
      Windows) раньше читались только как UTF-8 с `errors="ignore"` — вся
      кириллица терялась. Теперь: строгий UTF-8, при ошибке — cp1251, и
      только если оба не читаются — старый fallback с `errors="ignore"`.
      (Это был баг, найденный независимым агентом-ревьюером в Фазе 9,
      оставленный тогда как есть по договорённости с владельцем "это
      реально пофиксить или нет?" — да, пофикшено.)

      Протестировано вживую: полный DB-слой (temp SQLite) — видимость
      личных/общих дедлайнов на трёх разных пользователях, независимость
      персонального done (один отметил — у другого не изменилось),
      include_done, stats скоуплены на видимость+done, get_deadlines_soon
      персонально и shared_only, delete чистит deadline_done, миграция
      старого done=1 на отдельной "старой" базе без таблицы deadline_done.
      Реальная валидация формата времени на 15 граничных случаях (22.61,
      22.60, 24:00, 12:60 и т.д. — отклонены; 22.59, 09:30, 0:00 — приняты
      и нормализованы). Реальный роутинг через `Dispatcher.feed_update`
      (не прямые вызовы хендлеров — см. урок из Фазы 9): полный `/add` с
      фото на шаге описания (с фейковой Telegram-сессией без сети, реальные
      `Message`/`File`/`.as_(bot)`), обычный текстовый путь после этого не
      сломан, `/done` персонален и не протекает к другому user_id, `/del`
      общего дедлайна не старостой — отклонён. Фикс кодировки — round-trip
      на реальных cp1251/utf-8 байтах.
